@IsTest
private class GdContactTriggerHandlerTest {
  @IsTest
  static void constructor_createsInstance() {
    GdContactTriggerHandler handler = new GdContactTriggerHandler();
    System.assertNotEquals(null, handler);
  }
}